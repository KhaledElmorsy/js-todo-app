//Template Elements
const templates = {}
templates.newTodo = document.getElementById('new-todo')


// Containers
const containers = {}
containers.projectsContainer = document.getElementById('projects')
containers.todoContainer = document.querySelector('main')

const elements = {
    templates
    ,containers
}

export default elements
